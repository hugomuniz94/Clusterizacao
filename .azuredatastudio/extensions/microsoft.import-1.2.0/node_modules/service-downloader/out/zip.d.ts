import { EventEmitter2 as EventEmitter } from 'eventemitter2';
export declare type ExtractErrorType = 'CorruptZip' | 'Incomplete';
export declare class ExtractError extends Error {
    readonly type?: ExtractErrorType;
    readonly cause: Error;
    constructor(type: ExtractErrorType | undefined, cause: Error);
}
/**
 * Helper class to handle unzipping the contents of a zip archive into a specified directory.
 *
 * Code adapted from https://github.com/microsoft/vscode/blob/6ffa7d5887e60244169ef9699842ff276216be10/src/vs/base/node/zip.ts
 */
export declare class Unzipper {
    readonly eventEmitter: EventEmitter;
    extract(zipPath: string, targetPath: string): Promise<void>;
    private openZip(zipFile, lazy?);
    private extractZip(zipfile, targetPath);
    private extractEntry(stream, fileName, mode, targetPath, entryNumber, totalEntries);
    private openZipStream(zipFile, entry);
}
